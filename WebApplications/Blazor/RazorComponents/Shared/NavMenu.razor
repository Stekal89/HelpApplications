<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">RazorComponents</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        
        <!-- 
        Parent-Component: Included in Pages Folder: "ParameterParent.razor" 
        Child-Component (Used inside the Parent): Included in Shared Folder: "ParameterChild"
        -->
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="parameter-parent">
                <span class="oi oi-list-rich" aria-hidden="true"></span>Parent1
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="parameter-parent-2">
                <span class="oi oi-list-rich" aria-hidden="true"></span>Parent2
            </NavLink>
        </div>
        
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
